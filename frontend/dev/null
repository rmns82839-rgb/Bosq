import { useEffect, useState } from "react";
import { getElementosTabernaculo, getFigurasIglesia } from "../services/bibliaService";
import { VersiculoLink } from "../lib/bibliaLink";
import BotonPreguntarIA from "../components/common/BotonPreguntarIA";
const SECCIONES = {
  atrio: { label: "Atrio", color: "#A8823C", icon: "\u{1F525}" },
  lugar_santo: { label: "Lugar Santo", color: "#C9A84C", icon: "\u{1F54E}" },
  lugar_santisimo: { label: "Lugar Sant\xEDsimo", color: "#8B5CF6", icon: "\u2726" },
  estructura: { label: "Estructura", color: "#6B4A3A", icon: "\u{1F3D5}\uFE0F" },
  sacerdocio: { label: "Sacerdocio", color: "#60A5FA", icon: "\u{1F473}" }
};
const COLOR_FIGURA = "#5B8C6E";
export default function Tabernaculo() {
  const [vista, setVista] = useState("tabernaculo");
  const [elementos, setElementos] = useState([]);
  const [figuras, setFiguras] = useState([]);
  const [loading, setLoading] = useState(true);
  const [filtroSeccion, setFiltroSeccion] = useState("todas");
  const [busqueda, setBusqueda] = useState("");
  useEffect(() => {
    Promise.all([getElementosTabernaculo(), getFigurasIglesia()]).then(([els, figs]) => {
      setElementos(els);
      setFiguras(figs);
      setLoading(false);
    }).catch(() => setLoading(false));
  }, []);
  const secciones = [...new Set(elementos.map((e) => e.seccion).filter(Boolean))];
  const elementosFiltrados = elementos.filter((e) => {
    const matchSeccion = filtroSeccion === "todas" || e.seccion === filtroSeccion;
    const matchBusq = !busqueda || e.nombre.toLowerCase().includes(busqueda.toLowerCase()) || e.tipologiaCristo?.toLowerCase().includes(busqueda.toLowerCase()) || e.descripcion?.toLowerCase().includes(busqueda.toLowerCase());
    return matchSeccion && matchBusq;
  });
  const figurasFiltradas = figuras.filter(
    (f) => !busqueda || f.nombre.toLowerCase().includes(busqueda.toLowerCase()) || f.cristoEnLaFigura?.toLowerCase().includes(busqueda.toLowerCase()) || f.descripcion?.toLowerCase().includes(busqueda.toLowerCase())
  );
  const tabBtn = (id, texto) => /* @__PURE__ */ React.createElement("button", { onClick: () => {
    setVista(id);
    setBusqueda("");
    setFiltroSeccion("todas");
  }, style: {
    flex: 1,
    fontFamily: "var(--crimson)",
    fontSize: 15,
    padding: "10px 0",
    borderRadius: "var(--radius)",
    border: `1px solid ${vista === id ? "var(--gold)" : "var(--border2)"}`,
    background: vista === id ? "var(--gold-glow)" : "none",
    color: vista === id ? "var(--gold)" : "var(--text-muted)",
    cursor: "pointer"
  } }, texto);
  return /* @__PURE__ */ React.createElement("main", { style: { flex: 1, padding: "28px 32px 100px", maxWidth: 900, minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { marginBottom: 20 } }, /* @__PURE__ */ React.createElement("h1", { style: { fontFamily: "var(--crimson)", fontSize: 36, color: "var(--gold)", fontWeight: 300, marginBottom: 6 } }, "Tabern\xE1culo y la Iglesia"), /* @__PURE__ */ React.createElement("p", { style: { fontSize: 13, color: "var(--text-muted)", lineHeight: 1.6 } }, 'Cada elemento del tabern\xE1culo y cada figura de la Iglesia apuntan a Cristo \u2014 toca "Preguntar a la IA" para el estudio completo')), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, marginBottom: 20 } }, tabBtn("tabernaculo", `\u{1F3D5}\uFE0F Tabern\xE1culo (${elementos.length})`), tabBtn("figuras", `\u{1F54A}\uFE0F Figuras de la Iglesia (${figuras.length})`)), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, flexWrap: "wrap", marginBottom: 20 } }, /* @__PURE__ */ React.createElement(
    "input",
    {
      style: { background: "var(--surface2)", border: "1px solid var(--border2)", color: "var(--text)", fontFamily: "var(--sans)", fontSize: 13, padding: "8px 14px", borderRadius: "var(--radius)", outline: "none", minWidth: 180 },
      placeholder: vista === "tabernaculo" ? "Buscar elemento..." : "Buscar figura...",
      value: busqueda,
      onChange: (e) => setBusqueda(e.target.value)
    }
  ), vista === "tabernaculo" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("button", { onClick: () => setFiltroSeccion("todas"), style: {
    fontFamily: "var(--mono)",
    fontSize: 10,
    padding: "6px 14px",
    borderRadius: 4,
    border: `1px solid ${filtroSeccion === "todas" ? "var(--gold)" : "var(--border2)"}`,
    background: filtroSeccion === "todas" ? "var(--gold-glow)" : "none",
    color: filtroSeccion === "todas" ? "var(--gold)" : "var(--text-muted)",
    cursor: "pointer"
  } }, "Todas (", elementos.length, ")"), secciones.map((s) => {
    const info = SECCIONES[s] || { label: s, color: "var(--text-muted)", icon: "\u2022" };
    const count = elementos.filter((e) => e.seccion === s).length;
    return /* @__PURE__ */ React.createElement("button", { key: s, onClick: () => setFiltroSeccion(s), style: {
      fontFamily: "var(--mono)",
      fontSize: 10,
      padding: "6px 12px",
      borderRadius: 4,
      border: `1px solid ${filtroSeccion === s ? info.color : "var(--border2)"}`,
      background: filtroSeccion === s ? `${info.color}18` : "none",
      color: filtroSeccion === s ? info.color : "var(--text-muted)",
      cursor: "pointer"
    } }, info.icon, " ", info.label, " (", count, ")");
  }))), /* @__PURE__ */ React.createElement("div", { style: { fontFamily: "var(--mono)", fontSize: 10, color: "var(--text-muted)", marginBottom: 16 } }, loading ? "Cargando..." : vista === "tabernaculo" ? `${elementosFiltrados.length} elementos` : `${figurasFiltradas.length} figuras`), loading && /* @__PURE__ */ React.createElement("div", { className: "loading" }, /* @__PURE__ */ React.createElement("div", { className: "loading-dot" }), /* @__PURE__ */ React.createElement("div", { className: "loading-dot" }), /* @__PURE__ */ React.createElement("div", { className: "loading-dot" })), !loading && vista === "tabernaculo" && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(280px, 1fr))", gap: 12 } }, elementosFiltrados.map((e) => {
    const info = SECCIONES[e.seccion] || { label: e.seccion, color: "var(--gold)", icon: "\u2022" };
    return /* @__PURE__ */ React.createElement("div", { key: e.id, style: {
      background: "var(--surface)",
      borderTop: `3px solid ${info.color}`,
      borderRight: `1px solid ${info.color}25`,
      borderBottom: `1px solid ${info.color}25`,
      borderLeft: `1px solid ${info.color}25`,
      borderRadius: "var(--radius)",
      padding: "16px"
    } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "flex-start", gap: 10, marginBottom: 8 } }, /* @__PURE__ */ React.createElement("span", { style: { fontSize: 20 } }, info.icon), /* @__PURE__ */ React.createElement("div", { style: { flex: 1 } }, /* @__PURE__ */ React.createElement("div", { style: { fontFamily: "var(--crimson)", fontSize: 17, color: info.color, lineHeight: 1.2, marginBottom: 4 } }, e.nombre), /* @__PURE__ */ React.createElement("span", { style: {
      fontFamily: "var(--mono)",
      fontSize: 8,
      color: info.color,
      background: `${info.color}15`,
      border: `1px solid ${info.color}30`,
      borderRadius: 3,
      padding: "2px 8px",
      display: "inline-block"
    } }, info.label), e.cita && /* @__PURE__ */ React.createElement("div", { style: { fontFamily: "var(--mono)", fontSize: 9, color: "var(--text-muted)", marginTop: 6 } }, /* @__PURE__ */ React.createElement(VersiculoLink, { cita: e.cita })))), e.tipologiaCristo && /* @__PURE__ */ React.createElement("p", { style: { fontSize: 12, color: info.color, lineHeight: 1.5, margin: "0 0 8px" } }, "\u271D Cristo: ", e.tipologiaCristo), e.descripcion && /* @__PURE__ */ React.createElement("p", { style: { fontSize: 12, color: "var(--text-muted)", lineHeight: 1.6, margin: "0 0 10px" } }, e.descripcion), /* @__PURE__ */ React.createElement(BotonPreguntarIA, { tipo: "tabernaculo", datos: e, color: info.color }));
  })), !loading && vista === "figuras" && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(280px, 1fr))", gap: 12 } }, figurasFiltradas.map((f) => /* @__PURE__ */ React.createElement("div", { key: f.id, style: {
    background: "var(--surface)",
    borderTop: `3px solid ${COLOR_FIGURA}`,
    borderRight: `1px solid ${COLOR_FIGURA}25`,
    borderBottom: `1px solid ${COLOR_FIGURA}25`,
    borderLeft: `1px solid ${COLOR_FIGURA}25`,
    borderRadius: "var(--radius)",
    padding: "16px"
  } }, /* @__PURE__ */ React.createElement("div", { style: { fontFamily: "var(--crimson)", fontSize: 17, color: COLOR_FIGURA, lineHeight: 1.2, marginBottom: 4 } }, f.nombre), f.cita && /* @__PURE__ */ React.createElement("div", { style: { fontFamily: "var(--mono)", fontSize: 9, color: "var(--text-muted)", marginBottom: 8 } }, /* @__PURE__ */ React.createElement(VersiculoLink, { cita: f.cita })), f.cristoEnLaFigura && /* @__PURE__ */ React.createElement("p", { style: { fontSize: 12, color: COLOR_FIGURA, lineHeight: 1.5, margin: "0 0 8px" } }, "\u271D Cristo: ", f.cristoEnLaFigura), f.descripcion && /* @__PURE__ */ React.createElement("p", { style: { fontSize: 12, color: "var(--text-muted)", lineHeight: 1.6, margin: "0 0 10px" } }, f.descripcion), /* @__PURE__ */ React.createElement(BotonPreguntarIA, { tipo: "figura_iglesia", datos: f, color: COLOR_FIGURA })))));
}
